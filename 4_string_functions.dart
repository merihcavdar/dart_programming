void main(List<String> args) {
  String firstName = 'Merih';
  String surName = 'Çavdar';
  String fullName = firstName + ' ' + surName;
  print(firstName.length);
  print(surName.length);
  print(fullName.toUpperCase());
  print(fullName.toLowerCase());
  print(fullName);
  print(fullName.indexOf('h'));
  print(fullName.substring(0, 8));
}
